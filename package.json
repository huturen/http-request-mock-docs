{
  "name": "http-request-mock-docs",
  "version": "1.0.0",
  "description": "http-request-mock docs",
  "license": "MIT",
  "scripts": {
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress build src",
    "local-build": "vuepress build src",
    "clean-dev": "vuepress dev src --no-cache",
    "dev": "http-request-mock-cli --index http-request-mock.js -e none -d src/mock -w \"vuepress dev src\"",
    "eject-theme": "vuepress eject-hope src"
  },
  "devDependencies": {
    "vuepress": "1.8.2",
    "vuepress-theme-hope": "1.20.2"
  },
  "dependencies": {
    "axios": "^0.21.3",
    "http-request-mock": "^1.8.17",
    "localstorage-polyfill": "^1.0.1"
  }
}
